.tribe-events-pro {

	/* -----------------------------------------------------------------------------
	 * Event
	 * ----------------------------------------------------------------------------- */

	.tribe-events-pro-photo__event {
		margin-bottom: var(--spacer-5);

		.tribe-common--breakpoint-xsmall& {
			width: var(--grid-width-1-of-2);
		}

		.tribe-common--breakpoint-medium& {
			margin-bottom: var(--spacer-9);
			width: var(--grid-width-1-of-3);
		}
	}

	.tribe-events-pro-photo__event-featured-image-wrapper {
		flex: none;
		margin-bottom: var(--spacer-2);
	}

	.tribe-events-pro-photo__event-featured-image-link,
	.tribe-events-pro-photo__event-featured-image {
		display: block;
	}

	.tribe-events-pro-photo__event-details-wrapper {
		display: flex;
	}

	.tribe-events-pro-photo__event-date-tag {
		flex: none;
		width: 40px;
		margin-right: var(--spacer-2);
	}

	.tribe-events-pro-photo__event-date-tag-datetime {
		display: flex;
		flex-direction: column;
		text-align: center;
	}

	.tribe-events-pro-photo__event-date-tag-month {
		margin-bottom: calc(var(--spacer-0) * -1);
	}

	.tribe-events-pro-photo__event-details {
		flex: auto;
		margin-top: -3px;

		& > *:last-child {
			margin-bottom: 0;
		}
	}

	.tribe-events-pro-photo__event-title {
		margin: 0 0 var(--spacer-1);
	}

	.tribe-events-pro-photo__event-datetime-featured-icon {
		display: inline-block;
		margin-right: var(--spacer-0);
	}

	.tribe-events-pro-photo__event-datetime-featured-text {
		margin-right: var(--spacer-1);
	}

	.tribe-events-pro-photo__event-datetime-recurring-icon {
		display: inline-block;
		margin-left: var(--spacer-0);
	}

	.tribe-events-pro-photo__event-cost {
		margin-top: var(--spacer-1);

		.tribe-common--breakpoint-medium& {
			margin-top: var(--spacer-2);
		}
	}

	/* -----------------------------------------------------------------------------
	 * Theme Overrides - Twenty Nineteen
	 * ----------------------------------------------------------------------------- */

	.tribe-theme-twentynineteen & {

		.entry.tribe-common-g-col.tribe-events-pro-photo__event {
			margin-bottom: var(--spacer-5);
		}

		&.tribe-common--breakpoint-medium .entry.tribe-common-g-col.tribe-events-pro-photo__event {
			margin-bottom: var(--spacer-9);
		}
	}
}
